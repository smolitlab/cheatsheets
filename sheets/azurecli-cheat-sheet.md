# Azure CLI Cheat Sheet

    az login --service-principal -u $(appid) -p $(appsecret) --tenant $(tenant)
---   
    az account set --subscription $(subscription)
---   
    az acr login --name $(acrname)
